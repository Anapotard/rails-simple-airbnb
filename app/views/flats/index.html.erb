<div class="container mt-3">
  <div class="row">
    <div class="col-12">
      <h1>All our Flats (4)</h1>
    </div>
  </div>
<div class="d-flex justify-content-around flex-column">
  <ul class="list-group list-group-horizontal">
    <% @flats.each do |flat| %>
      <div class="list-group-item m-4 flex-wrap">
        <div class="flat">

          <h3><%= flat.name %></h3>
          <p><%= flat.address %></p>
          <p>👤<%= flat.number_of_guests %> 💲<%= flat.price_per_night %></p>
        </div>
        <div class="links d-flex justify-content-around">
          <%= link_to "More info", flat_path(flat), class: 'text-primary'%>
          <%= link_to "Edit", edit_flat_path(flat), class: 'text-warning' %>
          <%= link_to "Remove",
            flat_path(flat),
            method: :delete,
            data: { confirm: "Are you sure you want to delete #{flat.name}?" },
            class: 'text-danger' %>
        </div>
      </div>
    <% end %>
  </ul>
</div>
